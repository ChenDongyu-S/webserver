//项目上的一些问题


定时器：
创建计时器：仿照游双代码写的计时器只有在链接新建立的时候会开始计时，如果一个链接反复的发送请求，应该也要更新计时器的时间，
不仅如此，最重要的是在读取的时候也要更新，考虑到一些大文件会一直持续读取长达几个小时，所以每次读取操作的时候也要更新计时
器记录的时间。

线程池：一定要注意生产者消费者模式

输出ip地址要使用ip地址转化函数

链接类：大文件的读写操作一个要小心，游双树上的带读取的文件字节和待发送的文件字节没有处理好，自己要处理好。

